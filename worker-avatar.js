(()=>{
  'use strict';
  // Emergency stability mode: disable the injected enhancement layer during Admin Dashboard startup.
  // Existing dashboard HTML, sections and functions remain untouched.
})();
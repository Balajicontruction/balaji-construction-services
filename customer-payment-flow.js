(() => {
  'use strict';
  const UPI_ID='9785438345@axl',PAYEE='PRAKASH CHAND SWAMI',SEEN_KEY='balaji_customer_notifications_seen_signature';
  const $=id=>document.getElementById(id),moneyLocal=n=>'₹'+Number(n||0).toLocaleString('en-IN',{maximumFractionDigits:2});
  const projectName=p=>p?.project_name||p?.work_type||'Project';
  function toastSafe(m){if(typeof toast==='function')toast(m);else alert(m)}
  function projectFromCard(card){if(!card||typeof projects==='undefined')return null;const title=card.querySelector('h3')?.textContent?.replace(/^🏗️\s*/,'').trim();return projects.find(p=>projectName(p)===title)||null}
  function removeOldSelectors(){document.querySelectorAll('.amountOptions').forEach(el=>el.remove())}
  function validateAmount(project,raw){const amount=Number(String(raw??'').replace(/[^0-9.]/g,''));const due=Math.max(0,Number(project?.agreed_amount||0)-Number(typeof projectPaid==='function'?projectPaid(project.id):0));if(!Number.isFinite(amount)||amount<=0){toastSafe('सही payment amount दर्ज करें');return 0}if(due>0&&amount>due){toastSafe('Payment amount बाकी balance से अधिक नहीं हो सकता।');return 0}return amount}
  function openSubmit(project,rawAmount,method='UPI'){const amount=validateAmount(project,rawAmount);if(!amount)return;window.selectedProject=project;window.selectedAmount=amount;if($('paymentProjectName'))$('paymentProjectName').textContent=projectName(project);if($('selectedAmountLabel'))$('selectedAmountLabel').textContent=moneyLocal(amount);if($('paymentMethod'))$('paymentMethod').value=method;if($('paymentRef'))$('paymentRef').value='';if($('paymentNote'))$('paymentNote').value='';if($('paymentMsg'))$('paymentMsg').textContent='';const notice=document.querySelector('#paymentModal .notice');if(notice)notice.innerHTML='<b>Payment record:</b> UPI/Bank से payment करने के बाद अपना UTR/Reference Number डालें। Submit होते ही इस project की payment <b>🟢 Paid / Received</b> में save होगी।';if($('paymentModal'))$('paymentModal').classList.add('show')}
  function launchUpi(project,rawAmount){const amount=validateAmount(project,rawAmount);if(!amount)return;sessionStorage.setItem('balaji_upi_payment',JSON.stringify({projectId:String(project.id),amount,startedAt:Date.now()}));const upi='upi://pay?pa='+encodeURIComponent(UPI_ID)+'&pn='+encodeURIComponent(PAYEE)+'&am='+encodeURIComponent(amount.toFixed(2))+'&cu=INR&tn='+encodeURIComponent('BALAJI Construction - '+projectName(project));window.location.href=upi}
  function patchPaymentCards(){removeOldSelectors();document.querySelectorAll('.payCard').forEach(card=>{const project=projectFromCard(card),method=card.querySelector('.method');if(!project||!method)return;if(!method.querySelector('[data-customer-payment-amount]')){const wrap=document.createElement('div');wrap.setAttribute('data-customer-payment-amount','1');wrap.style.cssText='margin-top:12px';wrap.innerHTML='<label style="display:block;font-weight:800;font-size:13px;margin-bottom:6px">Payment Amount</label><input data-payment-amount-input type="number" min="1" step="0.01" placeholder="₹ Amount" style="margin-bottom:9px"><div style="display:flex;gap:8px;flex-wrap:wrap"><button type="button" class="btn primary" data-pay-upi>📲 Pay via UPI</button><button type="button" class="btn secondary" data-pay-bank>🏦 Bank Transfer Payment</button></div><div class="muted" style="margin-top:7px">Payment करने के बाद reference/UTR डालकर history में तुरंत Paid/Received record होगा।</div>';method.appendChild(wrap);wrap.querySelector('[data-pay-upi]').addEventListener('click',()=>launchUpi(project,wrap.querySelector('[data-payment-amount-input]').value));wrap.querySelector('[data-pay-bank]').addEventListener('click',()=>openSubmit(project,wrap.querySelector('[data-payment-amount-input]').value,'Bank Transfer'))}const oldLink=method.querySelector('a[href^="upi://pay"]');if(oldLink)oldLink.remove()})}
  async function submitPaymentDirect(){const project=window.selectedProject||(typeof selectedProject!=='undefined'?selectedProject:null),amount=Number(window.selectedAmount||(typeof selectedAmount!=='undefined'?selectedAmount:0));if(!project||!amount){toastSafe('Project और payment amount जरूरी है।');return}const ref=$('paymentRef')?.value.trim();if(!ref){if($('paymentMsg'))$('paymentMsg').textContent='Transaction / UTR / Reference Number जरूरी है।';return}const method=$('paymentMethod')?.value||'UPI',note=$('paymentNote')?.value.trim()||'',customerId=typeof customer!=='undefined'?customer?.id:null,currentUser=typeof user!=='undefined'?user:null,sbClient=typeof sb!=='undefined'?sb:null;if(!sbClient||!customerId||!currentUser){toastSafe('Customer session उपलब्ध नहीं है।');return}const{error}=await sbClient.from('payments').insert({project_id:project.id,customer_id:customerId,payment_date:new Date().toISOString().slice(0,10),amount,payment_method:method,reference_no:ref,notes:note||'Customer payment recorded as received.',created_by:currentUser.id,status:'received'});if(error){if($('paymentMsg'))$('paymentMsg').textContent='❌ Payment record save नहीं हुई: '+error.message;return}sessionStorage.removeItem('balaji_upi_payment');toastSafe('✅ Payment Paid / Received के रूप में save हो गई।');if(typeof closePaymentModal==='function')closePaymentModal();if(typeof loadPayments==='function')await loadPayments();if(typeof renderPayments==='function')renderPayments();if(typeof renderHome==='function')renderHome();if(typeof renderNotifications==='function')renderNotifications()}
  function installPaymentOverrides(){const oldRender=window.renderPayments;if(typeof oldRender==='function'&&!oldRender.__latestPaymentWrapped){const wrapped=function(){const r=oldRender.apply(this,arguments);setTimeout(patchPaymentCards,0);return r};wrapped.__latestPaymentWrapped=true;window.renderPayments=wrapped;setTimeout(patchPaymentCards,0)}window.submitPayment=submitPaymentDirect}
  function notificationSignature(){const list=$('notifyList');return list?list.textContent.trim():''}
  function installNotificationReadState(){const oldToggle=window.toggleNotifications;if(typeof oldToggle==='function'&&!oldToggle.__readStateWrapped){const wrapped=function(){const panel=$('notifyPanel'),wasHidden=panel?.classList.contains('hidden');const r=oldToggle.apply(this,arguments);if(wasHidden){localStorage.setItem(SEEN_KEY,notificationSignature());if($('notifyBadge')){$('notifyBadge').textContent='';$('notifyBadge').style.display='none'}}return r};wrapped.__readStateWrapped=true;window.toggleNotifications=wrapped}const oldRender=window.renderNotifications;if(typeof oldRender==='function'&&!oldRender.__readStateWrapped){const wrapped=function(){const r=oldRender.apply(this,arguments);const seen=localStorage.getItem(SEEN_KEY)||'',current=notificationSignature(),badge=$('notifyBadge');if(badge){if(seen&&current===seen){badge.textContent='';badge.style.display='none'}else if(current){badge.textContent='';badge.style.display='grid'}}return r};wrapped.__readStateWrapped=true;window.renderNotifications=wrapped}}
  function resumeAfterUpi(){const raw=sessionStorage.getItem('balaji_upi_payment');if(!raw)return;let p;try{p=JSON.parse(raw)}catch(e){sessionStorage.removeItem('balaji_upi_payment');return}if(!p||!p.projectId||!p.amount||Date.now()-Number(p.startedAt||0)>30*60*1000){sessionStorage.removeItem('balaji_upi_payment');return}const list=typeof projects!=='undefined'?projects:[],project=list.find(x=>String(x.id)===String(p.projectId));if(!project)return;sessionStorage.removeItem('balaji_upi_payment');setTimeout(()=>openSubmit(project,p.amount,'UPI'),350)}
  function contractStyles(){if(document.getElementById('balajiContractCustomerStyles'))return;const s=document.createElement('style');s.id='balajiContractCustomerStyles';s.textContent='.balajiContractBtn{margin-top:9px;width:100%;background:#0b2238!important;color:#fff!important}.balajiContractStatus{display:block;margin-top:8px;font-size:12px;color:#718096}.balajiContractLocked{color:#15803d;font-weight:800}.balajiContractPending{color:#c2410c;font-weight:800}';document.head.appendChild(s)}
  function contractStatusText(s){return ({amount_pending_customer:'Admin amount भेज चुका है — आपकी approval बाकी',amount_pending_admin:'Admin amount approval बाकी',revision_required:'Contract/Amount revision required',contract_pending_customer:'आपकी Contract approval बाकी',contract_pending_admin:'Admin final approval बाकी',locked:'🔒 Contract Locked',legacy_locked:'🔒 Existing Contract Locked'}[s]||'Contract उपलब्ध')}
  async function loadCustomerContracts(){if(typeof projects==='undefined'||!projects.length)return;const ids=projects.map(p=>p.id);const r=await sb.from('project_contracts').select('project_id,status,amount,amount_proposed_by').in('project_id',ids);window.__balajiCustomerContracts=r.error?[]:(r.data||[])}
  function patchContractButtons(){document.querySelectorAll('#projectCards .projectCard').forEach(card=>{const p=projectFromCard(card);if(!p)return;let row=card.querySelector('.balajiContractAction');const c=(window.__balajiCustomerContracts||[]).find(x=>String(x.project_id)===String(p.id));if(!c)return;if(!row){row=document.createElement('div');row.className='balajiContractAction';const progressBtn=card.querySelector('button[onclick^="showProgress"]');if(progressBtn)progressBtn.insertAdjacentElement('afterend',row);else card.appendChild(row)}row.innerHTML='<a class="btn primary balajiContractBtn" href="contract.html?project_id='+encodeURIComponent(p.id)+'">📄 Contract खोलें</a><span class="balajiContractStatus '+(c.status==='locked'||c.status==='legacy_locked'?'balajiContractLocked':'balajiContractPending')+'">'+esc(contractStatusText(c.status))+(c.amount!=null?' • '+moneyLocal(c.amount):'')+'</span>'})}
  async function installContractIntegration(){contractStyles();try{await loadCustomerContracts();patchContractButtons()}catch(e){console.warn('Contract integration:',e)}const oldRender=window.renderProjects;if(typeof oldRender==='function'&&!oldRender.__contractWrapped){const wrapped=function(){const r=oldRender.apply(this,arguments);setTimeout(async()=>{await loadCustomerContracts();patchContractButtons()},0);return r};wrapped.__contractWrapped=true;window.renderProjects=wrapped}setTimeout(async()=>{await loadCustomerContracts();patchContractButtons()},700);setTimeout(async()=>{await loadCustomerContracts();patchContractButtons()},1600)}
  function boot(){installPaymentOverrides();installNotificationReadState();patchPaymentCards();setTimeout(patchPaymentCards,700);setTimeout(patchPaymentCards,1600);setTimeout(resumeAfterUpi,900);setTimeout(installContractIntegration,900)}
  window.addEventListener('load',boot,{once:true});if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else setTimeout(boot,0);
})();